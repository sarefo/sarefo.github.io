# Only this site's own build noise belongs here. Sibling projects used to be
# physically nested in this repo, and every one of them needed a line below to
# keep git from swallowing it; they now live as siblings under C:\dev.

*.swp
*.swo
*.old
/node_modules
package-lock.json
